This directory is utilized by the HTCondor credd, condor_starter,
scitokens-credmon, and the web services associated with the scitokens-credmon
in order to manage tokens.

Each user or job with active tokens is represented by a sub-directory inside
this location; the HTCondor parameter SEC_CREDENTIAL_DIRECTORY should point
at this location.
